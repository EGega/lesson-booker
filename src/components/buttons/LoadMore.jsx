import styled from "./LoadMore.module.css"
const LoadMore = () => {
    return (
        <div>
            <button  className={styled.btn}>Load More</button>
        </div>
    )
}

export default LoadMore